<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.AnswerMapper">

    <resultMap id="AnswerResultMap" type="com.example.app.model.Answer">
        <id property="id" column="id" />
        <result property="questionId" column="question_id" />
        <result property="userId" column="user_id" />
        <result property="content" column="content" />
        <result property="isBest" column="is_best" />
        <result property="status" column="status" />  <!-- 添加status字段 -->
        <result property="createTime" column="create_time" />
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="AnswerResultMap">
        SELECT * FROM answers WHERE id = #{id} AND status != 'DELETED'
    </select>

    <select id="selectByQuestionId" parameterType="Long" resultMap="AnswerResultMap">
        SELECT * FROM answers WHERE question_id = #{qid} AND status != 'DELETED' ORDER BY create_time DESC
    </select>

    <!-- 新增：带权限过滤的查询 -->
    <select id="selectVisibleByQuestionId" resultMap="AnswerResultMap">
        SELECT * FROM answers
        WHERE question_id = #{questionId} AND status != 'DELETED'
        <if test="!isAdmin">
            AND (
                status = 'APPROVED'
                <if test="userId != null">
                    OR user_id = #{userId}
                </if>
            )
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 新增：按用户ID查询 -->
    <select id="selectByUserId" parameterType="Long" resultMap="AnswerResultMap">
        SELECT * FROM answers WHERE user_id = #{userId} AND status != 'DELETED' ORDER BY create_time DESC
    </select>

    <!-- 新增：按状态查询 -->
    <select id="selectByStatus" parameterType="String" resultMap="AnswerResultMap">
        SELECT * FROM answers WHERE status = #{status} AND status != 'DELETED' ORDER BY create_time DESC
    </select>

    <select id="selectBestAnswer" parameterType="Long" resultMap="AnswerResultMap">
        SELECT * FROM answers
        WHERE question_id = #{questionId} AND is_best = true
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.app.model.Answer" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO answers (question_id, user_id, content, is_best, status, create_time)
        VALUES (#{questionId}, #{userId}, #{content}, #{isBest}, #{status}, #{createTime})
    </insert>

    <update id="updateIsBest">
        UPDATE answers SET is_best = #{isBest} WHERE id = #{id}
    </update>

    <update id="clearBestAnswer">
        UPDATE answers SET is_best = false WHERE question_id = #{questionId}
    </update>

    <update id="updateStatus">
        UPDATE answers SET status = #{status} WHERE id = #{id}
    </update>

    <select id="countAnswers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM answers
    </select>

    <select id="countAnswersByStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM answers WHERE status = #{status}
    </select>

</mapper>